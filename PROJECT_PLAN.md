# 小型批发商订单—库存—对账系统｜项目方案

## 1. 项目定位

为 3—20 人的小型批发团队制作一套轻量经营系统，把分散在 Excel、聊天记录和纸质单据中的采购、销售、库存与欠款连接起来。

默认演示行业为五金耗材批发。公开时标注为“小宇 Mono 个人作品／演示项目”，演示企业、客户、商品、金额和经营数据均为虚构数据。

### 核心证明

证明小宇能够理解业务流程，并独立完成需求梳理、界面、前后端、测试、部署、备份和交接，而不只是制作后台页面。

### 目标客户

- 五金、耗材、服装辅料、食品和日用品批发商。
- 目前用多张表格管理库存、订单和欠款的小团队。
- 不需要完整 ERP，但希望减少重复录入和错账的经营者。

## 2. 启动前决策

开始写代码前确认并写回本节：

- 演示细分行业：默认五金耗材批发。
- 首版使用人数与角色数量。
- 技术栈、代码仓库和本地运行命令。
- 演示部署地址与数据重置方式。
- 首版时间盒；超出时间盒的功能移入后续清单。

完成标准：以上五项都有明确答案，且没有把支付、财税或硬件集成默认纳入首版。

## 3. 用户与权限

| 角色 | 主要操作 | 数据边界 |
|---|---|---|
| 老板／管理员 | 查看全部经营数据、配置账号和基础资料 | 全部数据 |
| 销售 | 建客户和销售单、查看自己负责的客户与回款 | 不能改库存和系统配置 |
| 仓库 | 收货、出库、退货、盘点 | 不查看客户欠款和利润 |
| 财务 | 查看应收应付、登记收付款、导出对账数据 | 不修改商品和库存流水 |

每个敏感操作同时做界面权限与服务端权限校验；权限失败必须返回清楚的错误，不依赖隐藏按钮保证安全。

## 4. 核心业务闭环

### 销售闭环

客户下单 → 销售创建订单 → 系统校验并预占库存 → 仓库出库 → 生成应收 → 财务登记全额或部分回款 → 订单完成。

### 采购闭环

库存低于预警值 → 创建采购单 → 收货入库 → 生成应付 → 财务登记付款 → 采购完成。

### 异常闭环

退货申请 → 审核 → 库存反向变动 → 应收／应付调整 → 保留原单和完整操作记录。

## 5. 首版范围

### 必做功能

- 商品、SKU、单位、分类和库存预警值。
- 客户、供应商和负责人。
- 采购单、销售单、入库、出库和退货。
- 当前库存、库存流水、盘点与预警。
- 应收、应付、部分收付款和对账记录。
- 老板、销售、仓库、财务四类权限。
- 操作日志和关键字段变更记录。
- 商品、客户和期初库存的 Excel 导入；订单与对账数据导出。
- 今日销售、库存预警、应收逾期和回款趋势看板。
- 移动端扫码查货；移动端出入库可根据时间盒决定是否纳入。

### 后续功能

- 报价单转销售单。
- 多仓库与仓库调拨。
- 批次、序列号和保质期。
- 打印模板和标签打印。
- 消息提醒与审批流配置。

### 明确不做

- 完整 ERP、MES、WMS 或财务总账。
- 税务申报、发票开具和银行自动对账。
- 真实支付、复杂促销和电商平台同步。
- PLC、电子秤、扫码枪等硬件深度集成。

## 6. 关键业务规则

1. 已确认的销售单预占库存；取消或退货时释放或反向变更。
2. 默认不允许产生负库存，管理员也必须通过调整单留下原因和日志。
3. 出库完成后按未收金额生成应收；允许多次部分回款，累计金额不能超过应收。
4. 所有库存变化只能通过业务单据或盘点调整产生，不能直接修改库存数字。
5. 已完成单据不能静默编辑；需要冲销或创建变更记录。
6. Excel 导入先预览错误行，确认后再写入；重复 SKU、缺失字段和非法数字必须指出具体行。
7. 删除基础资料前检查引用关系；已被业务单据引用的数据只允许停用。

## 7. 页面与数据

### 页面清单

- 登录与账号设置。
- 经营总览。
- 商品、客户、供应商列表与详情。
- 销售单、采购单的创建、详情和状态流转。
- 入库、出库、退货和盘点工作台。
- 库存查询与库存流水。
- 应收应付、收付款和对账详情。
- 导入中心、导出记录、操作日志和权限配置。

### 核心数据实体

`User`、`Role`、`Product`、`SKU`、`Customer`、`Supplier`、`Warehouse`、`StockLedger`、`SalesOrder`、`SalesOrderItem`、`PurchaseOrder`、`Inbound`、`Outbound`、`Return`、`Receivable`、`Payable`、`Payment`、`AuditLog`。

实体字段和关系以实际数据库迁移为准，本列表只固定业务边界，不缓存实现细节。

## 8. 演示数据与演示路径

准备不少于 30 个 SKU、8 个客户、5 个供应商、20 张历史订单和一组预警／逾期数据。提供一键重置脚本或种子命令。

固定演示路径：

1. 销售创建一笔包含两个 SKU 的订单。
2. 展示库存预占和仓库待出库任务。
3. 仓库完成出库，查看库存流水。
4. 财务看到自动生成的应收并登记部分回款。
5. 老板看板同步变化。
6. 创建一笔超过库存的订单，展示业务拦截。

完成标准：重置数据后连续运行三次，流程和关键数字一致。

## 9. 实施步骤与完成标准

1. **锁定流程**：画出销售、采购和退货三条状态流；每个状态的可执行角色、入口和结果均已写明。
2. **建立数据底座**：完成基础资料、权限和库存流水；自动化测试覆盖权限失败、重复 SKU 和负库存拦截。
3. **跑通交易闭环**：销售出库到部分回款、采购入库到付款均可完整操作，库存和账款能对上。
4. **补齐交付能力**：导入导出、日志、异常提示、响应式界面、部署和备份恢复均通过检查。
5. **准备公开证据**：演示数据可重置，截图和干净录屏齐全，README 写清作品性质、运行方式、功能边界和后续方向。

## 10. 项目验收标准

- 四类账号只能看到和执行各自权限内的操作。
- 一笔销售订单从创建到回款的数据变化可追溯，库存、应收和日志一致。
- 负库存、超额回款、重复导入和非法状态跳转均被拦截。
- 至少一份合法和一份含错误行的 Excel 导入文件通过验收。
- 核心流程有自动化测试，部署环境重建后可以恢复演示数据。
- 桌面端与常见手机宽度下没有阻断操作的布局问题。
- 项目说明、部署说明、备份恢复说明、关键截图和录屏全部存在。

## 11. 公开展示与服务承接

### 推荐展示材料

- “以前四张表，现在一笔订单自动联动库存和欠款”的前后对比。
- 销售、仓库和财务看到不同页面的权限演示。
- Excel 导入错误定位与库存异常拦截。
- 从需求、数据结构到部署交接的完整案例视频。

### 可承接服务

- 轻量进销存、订单、仓储和经营后台定制。
- Excel 数据迁移、字段定制、权限和报表。
- 扫码、打印、提醒、第三方接口和后续迭代。
- 部署上线、备份恢复与维护。

公开内容不承诺减少多少成本、提高多少效率或适配所有行业；使用真实操作证明已完成的能力。

## 12. 风险与边界

- 不同行业的库存计价、单位换算、批次和财务规则差异很大，正式报价前先确认业务规则。
- 演示账款不等于会计凭证，不能宣传为财税系统。
- 导入真实客户数据前必须提供脱敏、备份和回滚方案。
- 扫码和打印是否可用取决于具体设备、浏览器和驱动，需单独验证。

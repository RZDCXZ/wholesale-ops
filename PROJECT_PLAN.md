# 批发经营台账（Wholesale Ops）｜项目方案

## 1. 项目定位

为中国大陆小型五金耗材批发商制作一套轻量经营台账，将客户、销售单、库存和经营应收连接为一条可追溯的销售闭环。

本项目是小宇 Mono 的自由职业个人作品集项目，首要目标是证明能够独立完成业务梳理、交互与视觉、TypeScript 全栈实现、测试、本地交付和演示材料，从而承接轻量经营后台定制项目。第一评审人是批发商老板，第二评审人是技术评审者。

代码通过 GitHub 公开并采用 MIT License。项目只要求本地运行，不提供在线演示、不开放注册、不以获客数量作为完成标准。演示企业、客户、SKU、金额和经营数据全部为虚构数据，未经真实客户验证。

### 产品边界

- 产品名称使用“批发经营台账”，英文副标题使用“Wholesale Ops”。
- 不把项目宣传为 ERP、财税系统或经过真实企业验证的产品。
- 业务语境固定为简体中文、人民币、中国大陆联系方式与地址、中国标准时间。
- 项目以成果验收为终点，不设置时间盒；任何新增需求不得静默扩大首版范围。

## 2. 首版目标与完成定义

首版只跑通销售单—库存—经营应收闭环：

```text
销售单草稿
  → 确认并预占库存
  → 仓库完整出库
  → 生成经营应收
  → 一次或多次收款
  → 应收结清
```

首版在以下成果全部通过验收后完成：

- 核心销售闭环可以使用四类角色连续操作，库存、应收和业务审计数字一致。
- 商品与客户资料、期初库存、导入导出、经营总览和业务审计均可用。
- 权限失败、库存不足、并发预占、非法状态跳转和超额收款均被服务端拦截。
- 本地环境可初始化、重置、备份和恢复，公开仓库访客能够复现。
- 代码检查、类型检查、自动化测试和生产构建全部通过。
- README、License、流程图、截图和两种长度的演示视频全部存在。

采购、应付、退货、盘点和扫码不属于首版完成条件。

## 3. 用户与权限

首版使用老板、销售、仓库、财务四种固定角色，一个账号可以拥有多个角色，不提供自定义权限编辑器。

| 角色 | 主要操作 | 数据边界 |
| --- | --- | --- |
| 老板 | 查看全部经营数据；管理账号、角色、SKU、客户和导入；处理任意销售单与收款 | 全部数据 |
| 销售 | 维护自己负责的客户；创建、确认和取消相应销售单；查看相关收款进度 | 只能访问自己负责的客户及其销售业务，不能修改库存 |
| 仓库 | 查看库存和待出库任务；完成整单出库 | 只看履约所需客户名称、联系人、电话和地址，不看成交价与应收 |
| 财务 | 查看全部客户与应收；登记和撤销收款；导出应收 | 不能修改客户、SKU 和库存流水 |

每个服务端操作必须同时检查登录状态、角色和数据范围。页面隐藏或禁用按钮只改善体验，不能作为安全边界。越权操作返回清楚的错误。

种子数据提供五个演示账号：老板、销售、仓库、财务四个单角色账号，以及一个多角色账号。关闭公开注册、找回密码、邮件验证和第三方登录。

## 4. 领域规则

领域语言以 [CONTEXT.md](./CONTEXT.md) 为唯一术语来源。

### 4.1 SKU 与客户

- 一个 SKU 使用一个唯一且不可变的 SKU 编码和一个固定库存单位。
- 不同包装规格视为不同 SKU；数量只支持正整数，不支持单位换算或拆零。
- SKU 保存名称、分类、库存单位、参考售价、预警值和启用状态。
- 客户使用唯一且不可变的客户编码；客户名称允许重复。
- 客户保存联系方式、地址、一名当前客户负责人、默认账期和启用状态。
- 被业务记录引用的 SKU 和客户只能停用，不能删除。

### 4.2 库存

- 首版只有一个仓库。
- 现存量表示仓库实际持有数量，预占量表示已确认但尚未出库的承诺数量。
- `可用量 = 现存量 - 预占量`。
- 销售单确认时为所有明细一次性建立预占；任一 SKU 可用量不足则整单失败。
- 并发确认不能重复占用最后一份库存。
- 取消已确认销售单时释放预占，现存量不变。
- 出库时同时减少现存量和对应预占量。
- 所有数量变化必须留下库存流水，不能直接编辑现存量。
- 库存预警以启用 SKU 的可用量小于或等于预警值判断。
- 期初库存是库存流水起点，只能在尚未发生销售库存活动时导入。

### 4.3 销售单

销售单履约状态固定为：

| 状态 | 允许动作 | 结果 |
| --- | --- | --- |
| 草稿 | 编辑、删除、确认 | 尚未预占库存 |
| 已确认 | 取消、出库 | 内容冻结并建立预占 |
| 已出库 | 无后续履约动作 | 已完整交付，不能编辑、删除或取消 |
| 已取消 | 无 | 永久保留并已释放预占 |

- 销售可操作自己负责客户的销售单，老板可操作全部销售单。
- 一张销售单只允许一次完整出库，不支持部分预占、欠货或分批出库。
- 取消必须发生在出库前并填写原因。
- 销售单保存客户和账期快照，客户资料后续变化不影响历史交易。
- SKU 参考售价只提供默认值；草稿可修改成交价。
- 成交价采用人民币含税口径并精确到分，确认后冻结。
- 首版不计算税额、采购成本、毛利或利润。

### 4.4 应收与收款

- 本项目采用现场交货或自提的简化场景，出库完成即视为交付。
- 出库后按销售单全部成交金额生成一笔经营应收。
- 应收只是经营台账，不是会计凭证、收入确认或税务发票。
- 应收结算状态为待收款、部分收款、已结清；履约状态与结算状态相互独立。
- 到期日按出库日加销售单保存的账期计算；现结客户的到期日等于出库日。
- 超过到期日仍有未收金额的应收为逾期应收。
- 一笔收款只指定一笔应收，一笔应收允许多次部分收款。
- 收款金额必须大于零且不能超过当前未收金额。
- 收款记录日期、金额、方式、参考号和备注；方式固定为现金、银行转账、微信、支付宝和其他。
- 已登记收款不能编辑或删除。错误收款通过填写原因创建撤销收款反向记录，恢复未收金额并保留原记录。
- 首版不支持预收款、客户余额、跨单分摊和自动核销。

## 5. 首版功能与页面

### 必做功能

- 登录、退出、账号停用与固定角色分配。
- 经营总览。
- SKU 列表、详情、新建、编辑与停用。
- 客户列表、详情、新建、编辑、负责人调整与停用。
- 销售单列表、草稿创建与编辑、详情、确认和取消。
- 仓库待出库工作台与整单出库。
- 库存查询、现存量、预占量、可用量和库存流水。
- 应收列表、详情、部分收款、结清和撤销收款。
- SKU、客户和期初库存 `.xlsx` 导入。
- 销售单、应收和库存流水按当前筛选条件导出 `.xlsx`。
- 只追加的业务审计记录查询。

### 页面范围

- 登录与账号管理。
- 经营总览。
- SKU 管理。
- 客户管理。
- 销售单管理。
- 待出库工作台。
- 库存查询与库存流水。
- 应收与收款。
- 导入工作台。
- 业务审计记录。

桌面端是主要演示环境，重点验收 1440px 宽度。390px 手机宽度下常用查询和基础操作不能被布局阻断，但不要求扫码或移动出库。主验收浏览器为最新版 Chrome；不设正式无障碍或多浏览器认证目标。

## 6. 经营总览指标

所有日期按 `Asia/Shanghai` 计算：

| 指标 | 口径 |
| --- | --- |
| 今日销售额 | 当天完成出库的销售单成交金额合计 |
| 今日收款额 | 当天登记且未撤销的收款金额合计 |
| 未收金额 | 全部未结清应收的未收金额合计 |
| 逾期金额 | 全部逾期应收的未收金额合计 |
| 库存预警 | 可用量小于或等于预警值的启用 SKU 数量与明细 |
| 收款趋势 | 最近三十个自然日按收款日期汇总的有效收款金额 |

首版不展示成本、毛利或利润。

## 7. 导入导出

### 导入

- SKU、客户和期初库存分别使用固定 `.xlsx` 模板。
- 文件最大 10 MB，每次最多 2,000 行。
- 不接受宏，不计算公式，不保存上传的原始文件。
- 流程为解析、逐行校验、错误预览、用户确认、单事务写入。
- 任意一行存在错误时整批不写入。
- 重复编码、缺失字段和非法数值必须定位到具体行。
- 只创建新资料，不做模糊匹配或批量覆盖。

### 导出

- 销售单、应收和库存流水按当前权限和筛选条件导出 `.xlsx`。
- 不建设导出中心，不保存文件或导出历史。
- 每次导出写入业务审计记录。

## 8. 技术路线

- Node.js 24 LTS。
- pnpm 11，并提交 `pnpm-lock.yaml`、Node 主版本约束和 `.env.example`。
- Next.js 16 App Router 单体应用，只使用 Node.js runtime。
- 查询由服务端组件完成；写入口负责输入校验、鉴权和调用，不承载领域规则。
- 业务规则集中在独立纯 TypeScript 领域/应用服务中。
- Docker Compose 在本地运行 PostgreSQL 18，Next.js 在宿主机运行。
- Prisma ORM 7、Prisma Migrate 与 PostgreSQL 驱动适配器；提交 schema 和版本化 SQL migration。
- Better Auth 邮箱密码登录与数据库会话。
- Tailwind CSS 4、定制化 shadcn/ui、TanStack Table 和 Zod。
- SheetJS Community Edition 官方发行包在服务端处理 `.xlsx`。
- Pino 输出本地结构化运行日志。
- Vitest、Testcontainers PostgreSQL 和 Playwright 构成自动化测试体系。

确认、取消、出库、收款、撤销收款及其库存流水、应收和业务审计必须处于同一数据库事务中。库存竞争使用可重试的串行化事务。

## 9. 业务审计与运行日志

- 业务审计保存在 PostgreSQL，只追加、不编辑，并提供只读查询。
- 关键内容包括操作者、动作、对象、时间、原因、关联编号和必要变更摘要。
- 确认、取消、出库、收款、撤销收款、导入和导出均需记录。
- 业务审计不是事件溯源、防篡改系统或合规认证。
- Pino 运行日志只用于本地诊断，不接入 Sentry、云日志或外部监控。
- 密码、授权头、Cookie、会话、原始导入行和客户敏感字段不能进入日志。

## 10. 测试策略

测试只验证外部行为和稳定业务契约，不绑定组件树、ORM 调用顺序或私有实现细节。

### 测试接缝

1. **领域/应用服务**：使用 Vitest 验证状态跳转、金额、账期、权限和数据范围。
2. **应用服务与真实 PostgreSQL**：使用 Testcontainers 执行真实 migration，验证约束、事务、库存流水、业务审计和并发行为。
3. **浏览器关键旅程**：使用 Playwright 从登录开始验证角色权限、完整销售闭环、部分收款、撤销收款、Excel 导入和数据重置。

### 必测风险

- 重复 SKU 编码和客户编码。
- 草稿之外的静默编辑和非法状态跳转。
- 库存不足整单失败和并发预占最后库存。
- 取消释放预占、重复出库、已出库取消失败。
- 收款为零、负数或超过未收金额。
- 多次部分收款、结清、撤销和逾期判断。
- 四类角色的允许操作、拒绝操作和销售数据范围。
- Excel 错误定位、整批回滚和导出权限。
- 业务动作与业务审计的事务原子性。
- 连续三次演示数据重置后的确定性。

不以全仓覆盖率百分比为目标；每条关键业务规则必须同时具备成功和失败用例。提交和 Pull Request 通过 GitHub Actions 运行依赖安装、代码检查、类型检查、单元/集成测试和生产构建；关键 E2E 可使用独立工作流。

## 11. 本地运行与数据恢复

计划提供以下命令：

- `pnpm setup`：检查环境、启动数据库、执行 migration 并生成虚构演示数据。
- `pnpm dev`：启动本地应用。
- `pnpm demo:reset`：警告后销毁并重建本地演示数据。
- `pnpm db:backup`：使用 PostgreSQL 标准工具创建本地备份。
- `pnpm db:restore`：警告后用指定备份覆盖本地数据。

认证密钥在本地生成，不提交到 GitHub。重置和恢复是破坏性操作，执行前必须明确说明影响范围。

## 12. 演示数据与固定路径

每次重置生成：

- 30 个 SKU、8 个客户和 5 个演示账号。
- 20 张历史销售单。
- 至少 2 张待出库销售单。
- 至少 2 笔部分收款和 2 笔逾期应收。
- 至少 5 个库存预警 SKU。
- 一组库存刚好足够和一组库存不足场景。

日期相对重置当天生成，使“今日”和“逾期”场景持续有效；账号、数量、金额和场景规则保持确定。连续重置三次后关键数字一致。

固定演示路径：

1. 使用销售账号为自己负责的客户创建包含两个 SKU 的销售单草稿。
2. 确认销售单，展示现存量不变、预占量增加和可用量减少。
3. 使用仓库账号完成整单出库，展示库存流水和现存量变化。
4. 使用财务账号查看自动生成的应收并登记部分收款。
5. 撤销一笔收款，展示原记录保留和未收金额恢复。
6. 使用老板账号查看今日销售额、今日收款额、逾期金额、库存预警和业务审计变化。
7. 创建库存刚好足够的销售单并成功确认。
8. 创建超过可用量的销售单，展示整单拦截。
9. 切换角色，展示销售、仓库和财务不同的数据边界。
10. 导入一份含错误行的 `.xlsx`，展示逐行错误和整批不写入。

## 13. 公开展示材料

项目完成时必须提供：

- 中文 README：项目定位、业务边界、架构、本地运行、演示账号、业务规则、限制和后续路线图。
- MIT License。
- 销售—库存—经营应收流程图。
- 6—10 张关键截图。
- 60—90 秒结果导向短视频。
- 5—8 分钟完整业务闭环演示视频。
- 自动检查结果截图或状态徽章。

README 和个人网站必须明确声明：数据全部虚构、未经真实客户验证、不是财税系统、只支持本地运行、没有在线演示。

## 14. 实施阶段

1. **建立项目底座**：初始化技术栈、数据库、migration、认证、固定角色、本地脚本和自动检查。
2. **建立基础资料与库存**：完成 SKU、客户、期初库存、库存流水、权限与 Excel 导入。
3. **跑通销售履约**：完成销售单草稿、确认、并发预占、取消和整单出库。
4. **跑通经营应收**：完成应收、部分收款、结清、撤销收款和到期/逾期口径。
5. **补齐经营证据**：完成经营总览、导出、业务审计、日志脱敏、备份恢复和响应式布局。
6. **完成验收与公开材料**：补齐自动化测试、确定性重置、README、流程图、截图和视频。

每个阶段以可运行、可测试的纵向结果结束，不以页面数量或代码量作为进度。

## 15. 后续路线图与明确不做

### 后续路线图

- 供应商、采购、收货入库、应付与付款。
- 退货验收入库、应收调整和退款。
- 盘点、库存调整、补货、多仓库和调拨。
- 报价单、审批流、消息提醒、打印和移动扫码。
- 多库存单位、单位换算、批次、序列号和保质期。

### 明确不做

- 线上部署、公开注册、多租户、订阅计费和真实支付。
- 财务总账、会计凭证、税务申报、发票开具和银行自动对账。
- 客户价格等级、复杂促销、电商平台同步和硬件深度集成。
- 事件溯源、防篡改或合规认证。
- 正式 WCAG 认证和完整跨浏览器自动化矩阵。
- 对效率提升、成本下降或适配所有行业作未经验证的承诺。

## 16. 风险与限制

- 无真实从业者访谈，业务规则只代表本演示选择；所有公开材料必须如实说明。
- “出库即交付并生成应收”适用于本项目的现场交货或自提假设，不是行业通则。
- 首版没有采购和成本数据，因此不能展示可信利润。
- 首版没有退货或出库冲销；已出库错误只能通过重置演示数据恢复，第二阶段应优先补齐反向业务流程。
- 期初库存是唯一建库入口，首版不适合持续经营中的补货和盘点场景。
- 公开仓库和本地演示不能使用真实客户数据、真实凭据或未脱敏日志。

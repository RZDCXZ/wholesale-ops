## Agent skills

### Issue tracker

本仓库使用 `.scratch/` 下的本地 Markdown 文件跟踪 issue 和 spec。详见 `docs/agents/issue-tracker.md`。

### Triage labels

本仓库使用五个默认 triage 标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human` 和 `wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用 single-context 领域文档布局。详见 `docs/agents/domain.md`。

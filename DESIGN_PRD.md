# 批发经营台账（Wholesale Ops）｜设计 PRD

- Status: ready-for-design
- Version: V1
- Primary viewport: 1440 × 1024
- Responsive checkpoint: 390 × 844
- Language and locale: 简体中文、人民币、中国标准时间

## 0. 使用方式与需求来源

本文件用于生成信息架构、线框稿、高保真设计稿和可交互原型。它负责界面层决策，不重复定义业务规则、领域术语和工程实现。

开始设计或生成设计稿前，按以下分工读取需求源：

1. [CONTEXT.md](./CONTEXT.md)：界面术语的唯一来源。
2. [PROJECT_PLAN.md](./PROJECT_PLAN.md)：产品范围、业务规则、角色权限和完成定义的唯一来源。
3. [.scratch/wholesale-ops-v1/spec.md](./.scratch/wholesale-ops-v1/spec.md)：用户故事、实现决策和测试边界。
4. 本文件：信息架构、页面结构、交互表现、响应式策略和视觉验收标准。

发生冲突时，按以下优先级处理：

- 术语冲突：以 `CONTEXT.md` 为准。
- 业务行为、权限或范围冲突：以 `PROJECT_PLAN.md` 为准。
- 页面和交互表现冲突：以本文件为准。
- 无法由上述文档确定的产品行为：标记为待确认，不在设计稿中静默增加功能。

设计稿必须表现为可操作的中文经营后台，不制作官网、落地页、注册页、订阅页或营销模块。

## 1. 设计目标

### 1.1 核心结果

让批发商老板和项目评审者在较短时间内看懂并验证以下闭环：

```text
销售单草稿
  → 确认并预占库存
  → 完整出库
  → 自动生成应收
  → 一次或多次收款
  → 应收自动结清
```

界面必须持续回答四个问题：

1. 当前业务处于什么状态？
2. 当前用户可以执行什么动作？
3. 动作会改变哪些库存、金额或状态？
4. 变化之后可以在哪里追溯？

### 1.2 主要评审对象

- 批发商老板：判断业务是否容易理解、数字是否可信、操作是否符合日常语言。
- 技术评审者：判断权限、状态、数据联动和异常处理是否被真实表达。
- 项目作者：依靠确定的演示数据，稳定完成销售、库存、应收和审计演示。

### 1.3 设计成功标准

- 不阅读说明文档，也能从状态、主按钮和数字变化理解完整销售闭环。
- 销售单履约状态与应收结算状态始终分开展示，不使用笼统的“完成”。
- 现存量、预占量、可用量同时出现时，含义和变化方向清楚。
- 每个高风险操作在执行前说明结果，执行后给出可追溯入口。
- 不同角色看到的导航、字段和操作符合最小权限原则。
- 仓库界面不出现成交价、销售单金额、应收或收款信息。
- 1440px 下完整演示顺畅；390px 下约定的查询和基础操作不被布局阻断。

## 2. 设计原则

### 2.1 状态优先

- 页面标题区域优先展示单据编号、业务状态和下一步动作。
- 状态标签同时使用文字与颜色，不能只依赖颜色表达。
- 已冻结内容保持可读，但不表现为仍可编辑的表单。

### 2.2 数字可解释

- 金额、数量和日期使用一致的格式、对齐方式和精度。
- 会改变库存或未收金额的动作，确认前展示变化摘要。
- 总计与关键余额使用标签说明，不只显示孤立数字。

### 2.3 风险与操作权重匹配

- 页面只设置一个视觉最强的主操作。
- 确认销售单、出库、登记收款使用明确确认反馈。
- 取消销售单、撤销收款、停用账号、数据恢复等高风险动作使用危险样式并要求原因或二次确认。
- 导出、查看流水等低风险动作不与主业务动作争夺视觉权重。

### 2.4 角色聚焦

- 每种角色登录后进入自己的工作首页，不展示与职责无关的模块。
- 数据范围限制优先通过不展示无权数据实现；直接访问无权资源时显示明确拒绝页。
- 多角色账号使用权限并集，不要求用户切换“当前角色”。顶部账号菜单展示全部角色标签。

### 2.5 克制的信息密度

- 采用适合经营后台的中高信息密度，不制作大面积装饰性留白。
- 列表承担查找与比较，详情页承担解释与操作。
- 重要数字可以突出，但不使用夸张字号、渐变背景或营销式大卡片。

## 3. 已确定的界面决策

以下决策用于消除生成设计稿时的随机选择：

- 登录页与账号管理是两个独立页面。
- 老板首页为经营总览；销售首页为销售单列表；仓库首页为待出库工作台；财务首页为应收列表。
- 多角色账号展示合并后的导航和权限，不提供角色模式切换器。固定演示中的“切换角色”指退出后使用另一个演示账号登录。
- “已结清”是累计有效收款等于应收原始金额后自动得到的状态，不设置独立的“结清”写操作。
- 登记收款表单提供“填入全部未收金额”的快捷按钮，但最终仍通过“登记收款”提交。
- 列表、详情、新建和复杂编辑使用独立页面；确认、取消、出库、登记收款和撤销收款使用对话框或右侧抽屉。
- 一个仓库是系统固定前提。页面可以显示“默认仓库”，但不提供仓库选择器。
- 业务日期按 `Asia/Shanghai` 解释；界面不显示时区选择器。
- SKU 编码和客户编码创建后在编辑页以只读字段显示，并解释不可修改。
- 已确认、已出库和已取消销售单使用只读详情，不复用可编辑表单外观。
- 金额均为人民币含税金额；界面不出现税额、成本、毛利或利润。

## 4. 信息架构

### 4.1 桌面端框架

1440px 桌面端使用固定左侧导航和顶部工具栏：

- 左侧导航宽度约 240px，可折叠为图标栏。
- 顶部工具栏包含当前页面标题、必要的面包屑、账号菜单和角色标签。
- 页面内容区使用 24px 外边距；表格和表单的主要内容宽度优先服务信息可读性。
- 页面级主操作放在标题区域右侧。
- 筛选区位于列表标题下方；已启用的筛选以可清除条件显示。
- 详情页采用“标题与状态—摘要—主体内容—关联记录”的纵向结构。

### 4.2 导航分组

```text
经营
  经营总览

销售
  销售单
  客户
  SKU

仓库
  待出库
  库存

财务
  应收

数据
  导入
  业务审计

设置
  账号与角色
```

无权访问的导航项不显示。分组为空时连同分组标题一起隐藏。

### 4.3 页面可见性

| 页面或能力 | 老板 | 销售 | 仓库 | 财务 |
| --- | --- | --- | --- | --- |
| 经营总览 | 全部数据 | 不显示 | 不显示 | 不显示 |
| SKU | 查看、创建、编辑、停用 | 查看启用 SKU | 通过库存页查看必要资料 | 不显示 |
| 客户 | 全部查看和维护 | 仅负责客户，允许维护 | 仅在出库任务中看履约快照 | 全部只读 |
| 销售单 | 全部查看和处理 | 仅负责客户的销售单 | 仅待出库任务 | 从应收查看来源摘要 |
| 待出库 | 查看并可代为出库 | 不显示 | 查看并出库 | 不显示 |
| 库存与流水 | 全部查看 | 在选 SKU 时查看可用量 | 全部查看 | 不显示 |
| 应收与收款 | 全部查看和处理 | 只看相关收款进度 | 不显示 | 全部查看和处理 |
| 导入 | 使用 | 不显示 | 不显示 | 不显示 |
| 业务审计 | 查看 | 不显示 | 不显示 | 不显示 |
| 账号与角色 | 管理 | 不显示 | 不显示 | 不显示 |

所有权限在服务端继续校验；该表只定义页面呈现。

## 5. 全局交互规范

### 5.1 列表与筛选

- 列表默认展示搜索、最常用的 2—4 个筛选项和“更多筛选”。
- 搜索框允许使用业务编码或名称搜索；输入停顿后更新结果，也可按 Enter 提交。
- 筛选条件改变后返回第一页。
- 表头只对业务上有意义的列提供排序。
- 默认每页 20 条，允许切换 20、50、100 条。
- 空结果区分“系统暂无数据”和“当前筛选无结果”。
- 导出按钮导出当前权限和筛选条件下的数据，并在按钮附近说明范围。
- 行点击进入详情；行尾菜单只放次要操作，不能隐藏该页的主要动作。

### 5.2 表单

- 字段标签常驻，不使用占位文本代替标签。
- 必填项使用文字或符号说明，并在提交后把焦点移到第一个错误字段。
- 金额输入允许两位小数，失焦后格式化；数量只接受正整数。
- 不可变字段在编辑状态下使用只读控件，并解释不可修改的原因。
- 页面离开时若存在未保存修改，显示离开确认。
- 提交过程中禁用重复提交，并保留用户已经输入的内容。

### 5.3 状态与反馈

所有数据页面至少覆盖以下状态：

| 状态 | 表现要求 |
| --- | --- |
| 加载中 | 使用与最终结构接近的骨架屏，不用整页旋转图标 |
| 系统暂无数据 | 解释当前没有业务记录，并在有权限时提供创建入口 |
| 筛选无结果 | 保留筛选条件，提供清除筛选入口 |
| 字段校验失败 | 字段旁显示具体原因，顶部提供错误摘要 |
| 业务规则失败 | 说明失败对象、当前值、要求值和下一步建议 |
| 权限不足 | 显示无权访问的原因和返回可访问页面的按钮 |
| 资源不存在 | 显示记录可能不存在或已不可访问，不泄露数据是否真实存在 |
| 服务异常 | 保留上下文，提供重试；不展示堆栈或技术错误码 |
| 操作成功 | 更新页面数据并显示简短反馈；提供相关详情或流水入口 |

### 5.4 对话框与抽屉

- 简短确认使用居中对话框；包含多个字段的登记收款使用右侧抽屉。
- 对话框标题直接使用动作，例如“确认销售单”“取消销售单”。
- 危险操作按钮使用动作本身，不使用含糊的“确定”。
- 关闭后返回触发位置；提交失败时保留输入和打开状态。

### 5.5 格式

- 金额：`¥12,345.67`，表格中右对齐并使用等宽数字。
- 数量：`1,240`，只显示整数；旁边明确库存单位。
- 日期：`2026-08-13`。
- 日期时间：`2026-08-13 14:35`。
- 空值：使用 `—`，不使用 `0` 代替未知数据。
- 编码：保持原始大小写，使用适合辨认的等宽字体或等宽数字样式。

## 6. 核心流程

### 6.1 销售—库存—应收闭环

| 步骤 | 角色 | 页面 | 用户动作 | 必须显示的结果 |
| --- | --- | --- | --- | --- |
| 1 | 销售 | 销售单列表 | 新建销售单 | 进入草稿编辑页 |
| 2 | 销售 | 销售单编辑 | 选择客户和两个 SKU，填写数量与成交价 | 每行小计、总金额、可用量和账期快照 |
| 3 | 销售 | 确认对话框 | 确认销售单 | 内容冻结；现存量不变，预占量增加，可用量减少 |
| 4 | 仓库 | 待出库工作台 | 完成整单出库 | 现存量和预占量同时减少；显示库存流水入口 |
| 5 | 财务 | 应收详情 | 查看自动生成的应收 | 原始金额、到期日、未收金额和待收款状态 |
| 6 | 财务 | 收款抽屉 | 登记部分收款 | 累计收款增加、未收金额减少、状态变为部分收款 |
| 7 | 财务 | 应收详情 | 撤销一笔收款 | 原记录保留、出现反向记录、未收金额恢复 |
| 8 | 财务 | 收款抽屉 | 登记全部剩余金额 | 未收金额为零、状态自动变为已结清 |
| 9 | 老板 | 经营总览和业务审计 | 查看联动结果 | 指标、业务审计和相关明细保持一致 |

### 6.2 库存不足

1. 销售在草稿中录入超过可用量的数量。
2. 编辑页即时提示风险，但允许保存草稿。
3. 用户执行确认时，服务端再次检查库存。
4. 失败后保持草稿和输入不变。
5. 页面顶部显示“销售单未确认”，对应 SKU 行显示“需要数量 / 当前可用量 / 缺少数量”。
6. 不产生部分预占；页面提供返回修改数量的明确入口。

### 6.3 并发预占冲突

当编辑时可用量足够、提交确认时已被其他销售单占用：

- 使用“库存刚刚发生变化”的业务提示，不表现为系统崩溃。
- 展示最新可用量和受影响 SKU。
- 保持销售单为草稿，允许用户修改后再次确认。

### 6.4 导入

```text
选择导入类型
  → 下载并核对模板
  → 上传文件
  → 解析与逐行校验
  → 查看正确行与错误行
  → 全部通过后确认导入
  → 显示写入结果与审计记录入口
```

任一错误存在时，确认导入按钮保持禁用，并解释“整批不会写入”。

## 7. 页面清单

| ID | 页面 | 建议路由 | 主角色 |
| --- | --- | --- | --- |
| P01 | 登录 | `/login` | 全部 |
| P02 | 经营总览 | `/overview` | 老板 |
| P03 | 账号与角色 | `/settings/accounts` | 老板 |
| P04 | SKU 列表 | `/skus` | 老板、销售 |
| P05 | SKU 新建、编辑与详情 | `/skus/new`、`/skus/:id` | 老板、销售只读 |
| P06 | 客户列表 | `/customers` | 老板、销售、财务 |
| P07 | 客户新建、编辑与详情 | `/customers/new`、`/customers/:id` | 老板、销售、财务只读 |
| P08 | 销售单列表 | `/sales-orders` | 老板、销售 |
| P09 | 销售单草稿编辑 | `/sales-orders/new`、`/sales-orders/:id/edit` | 老板、销售 |
| P10 | 销售单详情 | `/sales-orders/:id` | 老板、销售；财务受限摘要 |
| P11 | 待出库工作台 | `/warehouse/outbound` | 老板、仓库 |
| P12 | 库存查询 | `/inventory` | 老板、仓库 |
| P13 | 库存流水 | `/inventory/ledger` | 老板、仓库 |
| P14 | 应收列表 | `/receivables` | 老板、财务 |
| P15 | 应收详情与收款 | `/receivables/:id` | 老板、财务；销售受限进度 |
| P16 | 导入工作台 | `/imports` | 老板 |
| P17 | 业务审计 | `/audit` | 老板 |

## 8. 页面规格

### P01 登录

**目标**：使用预置账号进入与角色相符的工作区。

**页面内容**：

- 产品名称“批发经营台账”和英文副标题“Wholesale Ops”。
- 明确说明“本地演示环境 · 数据均为虚构”。
- 邮箱、密码和“登录”按钮。
- 可选的演示账号说明入口；不展示注册、找回密码、邮箱验证或第三方登录。

**状态**：

- 空字段、邮箱格式错误、密码错误、账号停用、会话失效和服务异常。
- 登录失败不指出具体是邮箱还是密码错误。
- 登录成功后按角色进入默认首页。多角色账号按“老板 → 销售 → 仓库 → 财务”的顺序选择第一个可用首页。

### P02 经营总览

**目标**：让老板快速理解销售、收款、应收和库存风险。

**页面结构**：

1. 四张指标卡：今日销售额、今日收款额、未收金额、逾期金额。
2. 最近三十个自然日收款趋势图。
3. 库存预警摘要和明细表。

**交互**：

- 指标卡可点击进入带对应筛选条件的销售单或应收列表。
- 库存预警数量可进入库存页的“仅看预警”结果。
- 指标显示口径说明；“今日”按中国标准时间。
- 收款趋势图必须有坐标、数值提示和无数据状态。

**限制**：不增加成本、毛利、利润、采购或应付指标。

### P03 账号与角色

**目标**：由老板管理本地演示账号、启用状态和固定角色。

**列表列**：姓名、邮箱、角色、状态、操作。

**筛选**：姓名或邮箱、角色、启用状态。

**创建和编辑字段**：

- 姓名。
- 邮箱。
- 初始密码，仅创建时出现。
- 角色多选：老板、销售、仓库、财务，至少一个。
- 启用状态。

**交互**：

- 停用账号需要确认，并说明该账号将无法继续登录。
- 不设计自定义权限、邀请邮件、密码找回或第三方身份绑定。

### P04—P05 SKU

**列表目标**：查找 SKU 并比较库存状态。

**筛选**：

- SKU 编码或名称搜索。
- 分类。
- 启用状态。
- 仅看库存预警。

**列表列**：SKU 编码、名称、分类、库存单位、参考售价、现存量、预占量、可用量、预警值、状态、操作。

**表单字段**：

- SKU 编码，必填且创建后不可修改。
- 名称，必填。
- 分类，必填。
- 库存单位，必填。
- 参考售价，人民币，必填。
- 预警值，非负整数，必填。
- 启用状态。

**详情结构**：

1. 基本资料。
2. 库存数字摘要。
3. 最近库存流水。
4. 编辑、停用和查看完整流水入口。

**状态和规则**：

- 编码重复时在编码字段旁说明冲突。
- 被业务记录引用后，删除入口不出现，只允许停用。
- 停用 SKU 在历史记录中仍正常显示，在新销售单选项中不出现。
- 库存预警根据可用量判断，不能用现存量替代。

### P06—P07 客户

**列表目标**：按负责人和状态查找客户。

**筛选**：客户编码或名称、客户负责人、启用状态。

**列表列**：客户编码、客户名称、联系人、电话、客户负责人、默认账期、未收金额、状态、操作。

**表单字段**：

- 客户编码，必填且创建后不可修改。
- 客户名称，必填，允许重复。
- 联系人，必填。
- 电话，必填。
- 地址，必填。
- 客户负责人，必填。
- 默认账期：现结或指定天数。
- 启用状态。

**详情结构**：

1. 基本资料和负责人。
2. 销售概览：销售单数量、最近交易日期。
3. 应收概览：未收金额、逾期金额；销售只看自己负责客户，财务和老板按权限查看。
4. 相关销售单和应收入口。

**状态和规则**：

- 销售创建客户时，客户负责人默认为自己且不可改为他人。
- 负责人调整只对老板开放，并需要确认新的数据访问范围。
- 停用客户不再出现在新销售单客户选择器中，历史交易保持可读。
- 仓库不访问客户详情页，只查看销售单保存的履约快照。

### P08 销售单列表

**目标**：查找销售单并判断当前履约状态和下一步动作。

**筛选**：

- 销售单编号或客户名称。
- 履约状态：草稿、已确认、已出库、已取消。
- 创建日期范围。
- 客户负责人，仅老板显示。

**列表列**：销售单编号、客户、创建日期、客户负责人、明细数、成交金额、履约状态、更新时间、操作。

**默认排序**：更新时间降序。

**主操作**：“新建销售单”。

**行操作**：

- 草稿：编辑、确认、删除。
- 已确认：查看、取消；出库入口只对老板显示，销售不显示。
- 已出库、已取消：查看。

**空状态**：

- 系统无销售单：说明销售单会连接库存和应收，并提供新建入口。
- 筛选无结果：保留条件并提供清除筛选。

### P09 销售单草稿编辑

**目标**：在正式承诺履约前录入客户、SKU、数量和成交价。

**页面结构**：

1. 客户选择与客户快照预览。
2. 明细编辑区。
3. 金额与账期汇总。
4. 页面底部固定操作区：保存草稿、确认销售单。

**客户区域**：

- 只能选择当前用户可操作且已启用的客户。
- 选择后显示客户编码、联系人、电话、地址、客户负责人和默认账期。
- 明确提示确认后客户与账期信息将冻结为快照。

**明细列**：SKU、名称与单位、当前可用量、参考售价、数量、成交价、小计、删除。

**明细交互**：

- SKU 选择器支持编码和名称搜索，只显示启用 SKU。
- 选择 SKU 后带入参考售价作为成交价默认值。
- 数量为正整数；成交价精确到分。
- 同一 SKU 重复选择时提示合并或返回修改，不创建含糊的重复行。
- 即时显示可用量风险，但保存草稿不因库存不足被阻止。
- 至少保留一条有效明细才能确认。

**确认对话框**：

- 展示销售单编号、客户、明细数、成交总额。
- 展示“确认后内容冻结并预占库存”。
- 明确列出现存量不变、预占量增加、可用量减少。
- 主按钮使用“确认并预占库存”。

### P10 销售单详情

**目标**：解释销售单当前事实、库存影响和后续去向。

**标题区域**：销售单编号、履约状态、客户名称、创建时间和当前可用操作。

**主体内容**：

1. 客户与账期快照。
2. 冻结的销售明细和成交总额。
3. 履约时间线：创建、确认、取消或出库。
4. 库存影响摘要和相关库存流水入口。
5. 已出库时显示关联应收入口；销售只看结算进度，不展示无权的财务操作。

**取消对话框**：

- 仅已确认且未出库时出现。
- 原因为必填多行文本。
- 明确说明取消后释放全部预占、现存量不变且销售单永久保留。
- 主按钮使用“取消并释放预占”。

**状态**：

- 库存不足时保持草稿，并在详情顶部和对应 SKU 行显示具体差额。
- 已取消详情显示取消人、时间和原因。
- 已出库详情不展示编辑、删除或取消入口。

### P11 待出库工作台

**目标**：让仓库只处理已经确认、等待完整出库的任务。

**隐私边界**：整个页面及其对话框不出现成交价、金额、账期、应收或收款信息。

**筛选**：销售单编号或客户名称、确认日期范围。

**列表列**：销售单编号、客户名称、联系人、电话、地址摘要、确认时间、销售明细数、操作。

**任务详情**：

- 完整联系方式和地址。
- SKU 编码、名称、库存单位、应出数量。
- 当前预占是否完整。
- 确认时间和操作人。

**出库确认对话框**：

- 展示销售单编号、客户和逐 SKU 应出数量；不同库存单位的数量不合并为一个总数。
- 明确说明必须整单出库，不能修改数量。
- 明确说明现存量与预占量会同时减少，并生成一笔经营应收；不展示应收金额。
- 主按钮使用“完成整单出库”。

**状态**：任务已被其他人出库或取消时，从列表移除并显示清楚反馈，不保留失效操作按钮。

### P12—P13 库存与流水

**库存查询筛选**：SKU 编码或名称、分类、启用状态、仅看库存预警。

**库存列**：SKU 编码、名称、库存单位、现存量、预占量、可用量、预警值、库存状态、最近变化时间。

**数字表现**：

- 现存量、预占量、可用量并列展示。
- 表头提供简短解释，详情中展示 `可用量 = 现存量 - 预占量`。
- 可用量小于或等于预警值时显示文字标签“库存预警”。
- 不提供直接编辑数量、补货、盘点或库存调整按钮。

**库存流水筛选**：时间范围、SKU、流水类型、关联编号、操作者。

**流水类型**：期初库存、建立预占、释放预占、出库。

**流水列**：发生时间、SKU、类型、现存量变化、预占量变化、变化后现存量、变化后预占量、变化后可用量、关联对象、操作者。

**详情入口**：关联对象跳转到销售单或导入记录；流水本身只读。

### P14 应收列表

**目标**：让老板和财务识别待收、部分收款、已结清和逾期应收。

**筛选**：

- 应收编号、销售单编号或客户名称。
- 结算状态：待收款、部分收款、已结清。
- 仅看逾期。
- 到期日范围。
- 客户负责人。

**列表列**：应收编号、客户、销售单编号、原始金额、累计收款、未收金额、到期日、结算状态、逾期状态、操作。

**默认排序**：未结清优先，其次按到期日升序。

**状态表现**：

- “逾期”作为独立风险标签，与结算状态并列，不替代结算状态。
- 未收金额为零时自动显示已结清，登记收款入口隐藏。
- 导出按钮明确写出将按当前筛选条件导出。

### P15 应收详情与收款

**标题区域**：应收编号、结算状态、逾期标签、客户名称、未收金额和“登记收款”按钮。

**主体内容**：

1. 金额摘要：原始金额、有效累计收款、未收金额。
2. 到期信息：出库日、账期快照、到期日、逾期天数。
3. 来源销售单摘要和入口。
4. 收款与撤销记录时间线。

**登记收款抽屉字段**：

- 收款日期，默认当天。
- 金额，必须大于零且不能超过未收金额。
- “填入全部未收金额”快捷按钮。
- 收款方式：现金、银行转账、微信、支付宝、其他。
- 参考号，可选。
- 备注，可选。

**登记前摘要**：显示本次收款、登记后未收金额和预计结算状态。

**撤销收款**：

- 仅有效收款提供撤销入口。
- 原因为必填。
- 对话框显示原收款日期、金额、方式和撤销后的未收金额。
- 主按钮使用“撤销这笔收款”。
- 成功后原记录保留并标记已撤销，紧邻显示反向记录。

**限制**：不出现预收、客户余额、跨单分摊、自动核销、退款或删除收款。

### P16 导入工作台

**目标**：安全地导入 SKU、客户或期初库存，并在写入前解释全部错误。

**步骤**：

1. 选择类型：SKU、客户、期初库存。
2. 下载对应模板。
3. 拖放或选择 `.xlsx` 文件。
4. 显示文件名、大小、行数和解析状态。
5. 展示校验结果摘要与逐行结果表。
6. 全部通过后确认导入。
7. 展示成功数量与业务审计入口。

**校验结果表**：行号、字段或列名、原值、结果、错误原因。

**错误状态**：

- 文件超过 10 MB。
- 超过 2,000 行。
- 模板或工作表错误。
- 缺失字段、重复编码、非法整数、非法金额或公式。
- 任一错误存在时顶部强调“发现 N 条错误，整批不会写入”。

**期初库存限制**：系统已经发生销售库存活动时，上传区禁用，并解释期初库存只能作为库存流水起点。

### P17 业务审计

**目标**：让老板追溯关键业务动作，不把页面表现为安全合规或防篡改系统。

**筛选**：时间范围、操作者、动作、对象类型、关联编号。

**列表列**：发生时间、操作者、动作、对象、关联编号、原因或摘要。

**动作范围**：确认销售单、取消销售单、出库、登记收款、撤销收款、导入、导出。

**详情**：使用侧边抽屉展示必要变更摘要和关联对象入口；不展示敏感字段、原始导入行、会话或技术日志。

## 9. 响应式策略

### 9.1 390px 必须可完成

- 登录和退出。
- 查看角色默认首页。
- 搜索、筛选并查看 SKU、客户、销售单、库存和应收。
- 在权限允许时创建或编辑客户与 SKU 的基础资料。
- 创建、编辑和保存销售单草稿；销售明细转换为纵向卡片。
- 确认或取消销售单。
- 登记和撤销收款。

### 9.2 桌面优先能力

- 待出库工作台和完成出库。
- `.xlsx` 导入与逐行错误表。
- 账号与角色管理。
- 业务审计和批量导出。

这些页面在 390px 下至少能打开和查看说明，不要求完成复杂写操作；不得出现无法退出的弹窗或破坏整体布局的固定宽度内容。

### 9.3 移动布局规则

- 左侧导航变为顶部应用栏与抽屉导航。
- 页面主操作在需要时固定于底部安全区域。
- 数据表优先转换为信息卡；必须保留的宽表允许局部横向滚动，并固定首列。
- 筛选项收纳到全屏筛选抽屉，已启用条件在列表顶部可见。
- 对话框在移动端使用底部抽屉或全屏页面。
- 点击目标不小于 44 × 44px；金额和状态不得因截断失去含义。

## 10. 视觉方向

### 10.1 整体气质

关键词：可靠、清楚、克制、务实、可追溯。

界面应接近认真定制的经营工具，而不是未经调整的通用后台模板。允许使用 shadcn/ui 的交互基础，但需要统一页面密度、状态表达、表格、表单和数字样式。

### 10.2 初始设计令牌

| 用途 | 建议值 |
| --- | --- |
| 页面背景 | `#F6F7F9` |
| 表面背景 | `#FFFFFF` |
| 主文字 | `#17202A` |
| 次要文字 | `#667085` |
| 边框 | `#E4E7EC` |
| 主色 | `#2563EB` |
| 成功 | `#16803C` |
| 警告 | `#B54708` |
| 危险 | `#C62828` |
| 信息 | `#175CD3` |
| 常规圆角 | 8px |
| 控件高度 | 36—40px |
| 基础间距 | 4px / 8px 网格 |

状态色只作为辅助，所有状态都必须有文字标签或图标说明。

### 10.3 字体与图标

- 中文优先使用系统字体：`PingFang SC`、`Microsoft YaHei`、系统无衬线字体。
- 金额、数量和编码使用等宽数字特性。
- 图标使用 Lucide 或同等风格的线性图标，不使用 emoji 充当功能图标。
- 正文最小字号 14px；辅助文本不得低于 12px。

### 10.4 组件风格

- 卡片主要依靠边框和背景层级，不使用重阴影。
- 表格表头清晰但不过度加深；数值列右对齐。
- 状态标签使用浅色背景、深色文字和紧凑尺寸。
- 图表颜色克制，提供数值提示和文字图例。
- 主要按钮使用主色；危险动作只在真正不可逆或需要纠错时使用红色。
- 不使用玻璃拟态、霓虹渐变、大面积插画或营销式数据可视化。

## 11. 内容与演示数据

### 11.1 文案规则

- 使用 `CONTEXT.md` 中的正式术语，例如“销售单”“确认”“出库”“应收”“收款”“撤销收款”。
- 按钮使用动作加对象，例如“新建销售单”“登记收款”“完成整单出库”。
- 错误信息说明发生了什么、为什么失败以及下一步做什么。
- 不使用“订单”“发货”“支付”“回款”“欠款余额”等被领域词汇表排除的表达。
- 所有页面适当位置标明演示数据为虚构数据，但不反复打断主要操作。

### 11.2 跨页面一致的示例场景

设计稿优先复用以下虚构数据，保证数字在销售单、库存、应收和审计页面之间一致：

| 对象 | 示例值 |
| --- | --- |
| 销售单 | `XSD-20260813-0007` |
| 客户 | `KH-0003` · 广顺五金商行 |
| 客户负责人 | 陈敏 |
| 联系人 | 李海峰 · `138 0000 0000` |
| 地址 | 广东省深圳市宝安区工业路 18 号 |
| 账期 | 30 天 |
| SKU 1 | `WJ-LS-001` · 304 不锈钢六角螺栓 M8×30 · 盒 |
| SKU 1 数字 | 现存量 120、预占量 40、可用量 80、销售数量 20、成交价 ¥48.50 |
| SKU 2 | `WJ-QP-004` · 树脂切割片 105mm · 片 |
| SKU 2 数字 | 现存量 60、预占量 10、可用量 50、销售数量 30、成交价 ¥3.80 |
| 销售单总额 | `20 × ¥48.50 + 30 × ¥3.80 = ¥1,084.00` |
| 部分收款 | ¥400.00 |
| 未收金额 | ¥684.00 |

手机号、地址、企业、人员和金额均为虚构演示内容。

## 12. 无障碍基线

首版不声称通过正式 WCAG 认证，但设计稿必须满足以下基线：

- 正文与背景对比度至少 4.5:1；大号文字至少 3:1。
- 所有可交互控件具有可见的键盘焦点状态。
- 表单字段有常驻标签，错误信息与对应字段关联。
- 状态、预警和错误不只依靠颜色表达。
- 图表提供文字标题、图例和可读数值。
- 对话框打开后焦点进入对话框，关闭后回到触发控件。
- 动态成功或失败反馈可以被辅助技术识别。
- 移动端点击目标不小于 44 × 44px。

## 13. 设计稿生成批次

### 第一批：产品骨架与销售主流程

1. P01 登录。
2. P02 老板经营总览。
3. P08 销售单列表。
4. P09 新建销售单草稿。
5. P09 库存不足状态。
6. P10 已确认销售单详情和确认结果。

### 第二批：仓库与财务闭环

1. P11 仓库待出库工作台。
2. P11 整单出库确认。
3. P12 库存查询。
4. P13 库存流水。
5. P14 应收列表。
6. P15 部分收款详情、登记收款抽屉和撤销收款状态。

### 第三批：基础资料、导入与治理

1. P04—P05 SKU。
2. P06—P07 客户。
3. P03 账号与角色。
4. P16 导入错误预览和成功结果。
5. P17 业务审计。
6. 第一、二批关键页面的 390px 版本。

每批先完成 1440px 正常状态，再补充业务失败、空数据、无权限和 390px 状态。生成下一批前，应确认前一批的导航、组件和令牌已经固定。

## 14. 设计验收清单

### 14.1 产品和流程

- [ ] 17 个页面均有明确入口、目标角色和返回路径。
- [ ] 销售单确认、取消、出库、收款和撤销收款均展示动作结果。
- [ ] 履约状态与结算状态分开表达。
- [ ] 库存不足和并发冲突不会被设计成部分成功。
- [ ] 已冻结记录没有可编辑暗示。
- [ ] 仓库所有界面均未泄露价格、金额和应收。
- [ ] 销售只能看到自己负责客户的相关数据。

### 14.2 页面完整性

- [ ] 列表包含搜索、筛选、排序、分页、空状态和导出范围说明。
- [ ] 表单包含必填、校验、提交中、提交失败和未保存离开状态。
- [ ] 详情包含状态、主体事实、关联记录和下一步动作。
- [ ] 高风险动作包含明确结果、原因字段或二次确认。
- [ ] 加载、系统无数据、筛选无结果、权限不足和服务异常均有设计。

### 14.3 视觉和响应式

- [ ] 所有页面使用同一套导航、颜色、字体、间距、表格和表单规则。
- [ ] 金额、数量、编码和日期格式一致。
- [ ] 状态不只依靠颜色。
- [ ] 1440px 下没有无意义的大面积留白或拥挤操作区。
- [ ] 390px 约定操作可完成，内容不被固定宽度或弹窗阻断。
- [ ] 关键演示数据在页面之间保持数字一致。

## 15. 非设计范围

设计稿不包含以下能力：

- 采购、供应商、应付、退货、退款、盘点、补货和库存调整。
- 分批出库、欠货、部分预占、多仓库和库位。
- 预收款、客户余额、跨单分摊、自动核销和支付平台集成。
- 税额、发票、成本、毛利、利润和会计凭证。
- 自定义角色、权限编辑器、审批流和消息中心。
- 公开注册、找回密码、第三方登录、多租户、订阅和在线支付。
- 移动扫码、移动出库和硬件集成。
- 官网、营销页、帮助中心、在线客服和云端运维后台。

新增能力应先回到 `PROJECT_PLAN.md` 判断是否进入首版范围，再更新本文件；不得只在设计稿中增加看似合理但未定义的功能。
